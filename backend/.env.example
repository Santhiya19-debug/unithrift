# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Configuration
MONGO_URI=mongodb+srv://unithrift_user:unithrift%4069@cluster0.tyoowwc.mongodb.net/unithrift?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY=7d

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:5173

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Admin Email (for initial setup)
ADMIN_EMAIL=admin.unithrift@artvip.ac.in